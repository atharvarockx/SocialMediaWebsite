<%-include ('./includes/header.ejs') %>

<div class="container py-md-5 container--narrow">
    <%-include ('./includes/flash.ejs') %>
    <%-include ('./includes/profile-shared.ejs') %>
  
    <div class="list-group">
      <% k=0%>
      <% following.forEach(function(followed){ %>
        <a href="/profile/<%= followed.followedUsername %>" class="list-group-item list-group-item-action">
          <img class="avatar-tiny" src="<%=avatars[k]%>">
          <strong><%= followed.followedUsername %></strong> 
        </a>
        <% k=k+1%>
      <% }) %>
      
    </div>
  </div>

  <%-include ('./includes/footer.ejs') %>